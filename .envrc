# direnv configuration — auto-loads a local .env if present (no error if absent).
# This file is committed and MUST NOT contain secrets. Secrets go in .env (git-ignored).
# Run `direnv allow` once after cloning.
dotenv_if_exists .env
