# Tasks — add-site-repo-link

- [x] Profile schema + content: optional `repository` URL
- [x] Header: GitHub mark (inline SVG) left of the theme toggle, opens the repo in a new tab
- [x] Footer: "GitHub" link targets the repository (contact page keeps the personal profile)
- [x] DoD: vitest green, `make lint` clean, `make build` green, visual check
