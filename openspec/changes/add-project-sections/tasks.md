# Tasks — add-project-sections

- [x] Zod `projects` schema + `ProjectData` contract: optional `sections` (title, body paragraphs,
      image, media)
- [x] Extract `ProjectMedia.astro` (embeds + descriptions + timecodes + links) and reuse it at
      project level and per section
- [x] Section rendering: auto-numbered headings, body paragraphs, illustration, media
- [x] Content: Kpler sections (2 placeholders + Cushing drone inventories with pitch, image, video)
- [x] Asset: `kpler-cushing-drone-inventories.webp` stored locally under `public/assets`
- [x] DoD: vitest green, `make lint` clean, `make build` green, visual check on the page
